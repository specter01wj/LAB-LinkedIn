/**
 * Note: This file is intentionally empty.
 * You can use it to test your skills at traversing the DOM using JavaScript.
 */
const queryArticle = document.querySelectorAll('article');
const queryBackpack = document.querySelectorAll('.backpack');

const selectElem = document.getElementsByClassName('backpack__strap');

console.log(queryArticle);
console.log(queryBackpack);
console.log(selectElem);