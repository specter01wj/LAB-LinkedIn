
const divide = (x, y) => { 
    if(y === 0) {
        console.log('Error: dividing by 0!');
        return null;
    }

    return x / y;
}

console.log(divide(7, 0));

const divide2 = (x, y) => x / y;















